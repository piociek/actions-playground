name: Environment variable workflow
on: workflow_dispatch #manual trigger
env:
  ENV_NAME: ${{ secrets.SECRET_KEY }}
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v6

      - name: Setup java
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'
          architecture: x64

      - name: Setup gradle
        uses: gradle/actions/setup-gradle@v5

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Run tests
        run: ./gradlew test

      - name: Print env var (should fails since it's secret)
        run: echo "ENV_NAME = ${{ env.ENV_NAME }}"
